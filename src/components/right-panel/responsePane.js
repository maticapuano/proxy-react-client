import React, { Component } from 'react';
import { Tabs  } from 'antd';
import './style.scss';

const { TabPane } = Tabs;

class ResponsePane extends Component {
    render() {
        return (
            <div className="content-panel response-pane">
                response-pane
            </div>
        )
    }
}
export default ResponsePane;